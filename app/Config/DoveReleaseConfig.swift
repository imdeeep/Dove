import Foundation

enum DoveReleaseConfig {
    static let websiteURL = URL(string: "https://dove.duckdns.org")!
    static let versionCheckURL = URL(string: "https://dove.duckdns.org/version.json")!
    static let downloadURL = URL(string: "https://dove.duckdns.org/download")!
}
