import Foundation

enum DoveReleaseConfig {
    static let websiteURL = URL(string: "https://dove.imdeeep.in")!
    static let versionCheckURL = URL(string: "https://dove.imdeeep.in/version.json")!
    static let downloadURL = URL(string: "https://dove.imdeeep.in/download")!
}
